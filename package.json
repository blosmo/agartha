{
  "name": "agartha",
  "private": true,
  "version": "0.1.0",
  "description": "First playable Agartha demo loop: authoritative cellular world, viewer, and scripted agents.",
  "type": "module",
  "workspaces": [
    "apps/web",
    "packages/cli",
    "packages/protocol"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "dev:all": "node scripts/dev-all.mjs",
    "agents": "node --import tsx scripts/agents/run_scripted_agents.ts",
    "build": "npm --workspace packages/protocol run build && npm --workspace packages/cli run build && npm --workspace apps/web run build && npm run build:scripts",
    "build:scripts": "tsc --noEmit -p tsconfig.scripts.json",
    "test": "npm --workspace packages/protocol run test && npm --workspace packages/cli run test && npm --workspace apps/web run test && npm run test:scripts && npm run test:convex",
    "test:scripts": "vitest run --dir scripts",
    "test:convex": "vitest run --dir convex",
    "test:rust": "cargo test --workspace",
    "test:all": "npm run test && npm run test:rust"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "convex": "^1.37.0"
  }
}
